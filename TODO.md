# TODO

## Step 1 — Locate missing image fields
- [x] Confirmed Hero/About/Gallery image paths used in code.
- [x] Verified those referenced images exist in `brenda-protocol/public/`.
- [ ] Identify the exact UI component/section where images are missing (needs screenshot or component name).

## Step 2 — Fix image paths
- [ ] Copy/standardize missing images into `brenda-protocol/public/` OR update code to use correct filenames.

## Step 3 — Add safe fallbacks (optional)
- [ ] Add placeholder/fallback image if a file fails to load.

## Step 4 — Test
- [ ] Run `npm run lint`
- [ ] Run `npm run build`
- [ ] Verify images render in browser

